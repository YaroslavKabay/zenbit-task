import { FC } from "react";
import {FormElement} from "../../components";
import {FormPageDiv} from "../../App.styles";

const FormPage: FC = () => {
    return (
        <FormPageDiv>
            <FormElement/>
        </FormPageDiv>
    );
};

export  {FormPage};