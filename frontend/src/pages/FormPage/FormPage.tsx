import { FC } from "react";
import {FormElement, Forms} from "../../components";

const FormPage: FC = () => {
    return (
        <div>
            <FormElement/>
            <Forms/>
        </div>
    );
};

export  {FormPage};